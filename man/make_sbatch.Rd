% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sbatch.R
\name{make_sbatch}
\alias{make_sbatch}
\title{Generate (and optionally submit) a Slurm batch script for an R job}
\usage{
make_sbatch(
  r_script,
  job_name = "rjob",
  cpus = 8L,
  mem = "64G",
  time = "12:00:00",
  partition = NULL,
  array = NULL,
  r_args = character(0),
  env = "myR",
  conda_sh = "/share/apps/anaconda3/2025.06/etc/profile.d/conda.sh",
  log_dir = "logs",
  email = NULL,
  mail_type = "END,FAIL",
  extra_sbatch = character(0),
  out_file = NULL,
  submit = FALSE
)
}
\arguments{
\item{r_script}{Path to the R script to run. Must exist.}

\item{job_name}{Slurm job name. Defaults to \code{"rjob"}.}

\item{cpus}{Number of CPUs per task. Defaults to \code{8L}.}

\item{mem}{Memory allocation string (e.g., \code{"64G"}). Defaults to \code{"64G"}.}

\item{time}{Wall-time limit in \code{"HH:MM:SS"} format. Defaults to
\code{"12:00:00"}.}

\item{partition}{Slurm partition name. If \code{NULL} (default), no
\code{--partition} directive is written.}

\item{array}{Slurm array specification (e.g., \code{"0-9"}). If \code{NULL}
(default), no \code{--array} directive is written.}

\item{r_args}{Character vector of additional arguments passed to \code{Rscript}.
Defaults to \code{character(0)}.}

\item{env}{Conda environment name to activate before running the script.
Defaults to \code{"myR"}.}

\item{conda_sh}{Path to the conda \code{conda.sh} initialisation script.}

\item{log_dir}{Directory for Slurm log files. Created if it does not exist.
Defaults to \code{"logs"}.}

\item{email}{Email address for Slurm notifications. If \code{NULL} (default), no
mail directives are written.}

\item{mail_type}{Slurm mail event types. Defaults to \code{"END,FAIL"}.}

\item{extra_sbatch}{Character vector of additional raw \verb{#SBATCH} directives
to append.}

\item{out_file}{Path for the generated \code{.slurm} script. Defaults to a file
named \verb{<job_name>.slurm} in the same directory as \code{r_script}.}

\item{submit}{Logical. If \code{TRUE}, submits the script via \code{sbatch}
immediately. Defaults to \code{FALSE}.}
}
\value{
Invisibly returns the path to the written \code{.slurm} file.
}
\description{
Writes a Slurm batch script that activates a conda environment and runs an R
script via \code{Rscript}. Optionally submits the job immediately with \code{sbatch}.
}
\examples{
\dontrun{
make_sbatch("job.R", job_name = "my_job", cpus = 4, mem = "32G",
            email = "user@example.com", submit = TRUE)
}
}
