% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batchify.R
\name{batchify}
\alias{batchify}
\title{Convert a notebook to an R script and submit it as a Slurm batch job}
\usage{
batchify(notebook, job_name, run_root, mode = "exclude", submit = FALSE, ...)
}
\arguments{
\item{notebook}{Path to the notebook file (\code{.R}, \code{.Rmd}, \code{.qmd}, or
\code{.ipynb}).}

\item{job_name}{A short name for the Slurm job. Also used as the name of the
run subdirectory prefix.}

\item{run_root}{Path to the root directory where timestamped run directories
are created. Must be supplied by the user.}

\item{mode}{Passed to \code{\link[=capture_code]{capture_code()}}. Either \code{"exclude"} (default) to
strip code between \code{batch:start}/\code{batch:end} markers, or \code{"include"} to
keep only that code.}

\item{submit}{Logical. If \code{TRUE}, the Slurm script is submitted immediately
via \code{sbatch}. Defaults to \code{FALSE}.}

\item{...}{Additional arguments passed to \code{\link[=make_sbatch]{make_sbatch()}} (e.g., \code{cpus},
\code{mem}, \code{time}, \code{email}).}
}
\value{
Invisibly returns the path to the generated \code{.slurm} script.
}
\description{
Creates a timestamped run directory, extracts R code from a notebook using
\code{\link[=capture_code]{capture_code()}}, writes it to \code{job.R}, and generates (and optionally
submits) a Slurm batch script via \code{\link[=make_sbatch]{make_sbatch()}}.
}
\examples{
\dontrun{
batchify("analysis.qmd", job_name = "my_analysis", cpus = 4, mem = "32G")
}
}
