% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/capture.R
\name{capture_code}
\alias{capture_code}
\title{Extract R code from a notebook file}
\usage{
capture_code(
  notebook,
  mode = c("exclude", "include"),
  start = "#\\\\| ?batch:start",
  end = "#\\\\| ?batch:end"
)
}
\arguments{
\item{notebook}{Either a path to a notebook file or a character vector of R
code lines. Supported file types: \code{.R}, \code{.Rmd}, \code{.qmd}, \code{.ipynb}.}

\item{mode}{One of \code{"exclude"} (default) or \code{"include"}.
\itemize{
\item \code{"exclude"}: returns all code \emph{except} blocks wrapped in
\code{batch:start}/\code{batch:end} markers.
\item \code{"include"}: returns \emph{only} the code inside
\code{batch:start}/\code{batch:end} markers.
}}

\item{start}{Regular expression matching the batch-start marker comment.
Defaults to \code{"#\\\\| ?batch:start"}.}

\item{end}{Regular expression matching the batch-end marker comment.
Defaults to \code{"#\\\\| ?batch:end"}.}
}
\value{
A character vector of R code lines.
}
\description{
Reads code from an \code{.R}, \code{.Rmd}, \code{.qmd}, or \code{.ipynb} file (or a character
vector of code lines) and returns a character vector of R code lines.
Supports selective extraction via paired \verb{#| batch:start} / \verb{#| batch:end}
marker comments.
}
\examples{
\dontrun{
# Extract all code except marked blocks
code <- capture_code("analysis.qmd", mode = "exclude")

# Extract only marked blocks
code <- capture_code("analysis.qmd", mode = "include")
}
}
